Create a Svelte 5 component that demonstrates the `{#each}` block.

The component should display a hardcoded list of 3 Seinfeld characters (Jerry, Elaine, Kramer) and include a button to add George to the list.
